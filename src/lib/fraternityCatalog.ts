/**
 * Fraternity family labels are repeated across many profiles (facet counts aggregate).
 * Edit `FRATERNITY_FAMILY_LABELS` to match your chapter’s five families.
 *
 * Class filter options on Explore come from the `classes` org facet (database), not a static list.
 */

export const FRATERNITY_FAMILY_LABELS: readonly [string, string, string, string, string] = [
  'Brotherhood',
  'Unity',
  'Service',
  'Integrity',
  'Knowledge',
]
